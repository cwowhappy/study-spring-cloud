eureka:
  client:
    serviceUrl:
      defaultZone: http://@eureka.server.host@:@eureka.server.port@/eureka

server:
  port: @eureka.service.ribbon.port@

spring:
  application:
    name: @eureka.service.ribbon.name@


services:
  service-01:
    name: @eureka.service.01.name@
